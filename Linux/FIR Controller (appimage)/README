1. Copy the executable to usr/bin/
2. Donwload linuxdeployqt appimage, place it in ~/.local/bin and reneme it to linuxdeployqt.
3. Run (check if qmake path is set properly):
linuxdeployqt "usr/share/applications/FIR_Controller.desktop" -appimage -bundle-non-qt-libs -verbose=1 -no-copy-copyright-files -qmake=/usr/lib/qt5/bin/qmake -extra-plugins=platformthemes,styles -unsupported-allow-new-glibc
